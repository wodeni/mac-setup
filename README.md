# Nimo's New Mac Setup

## Setup

Inspect `init.sh` and change `EMAIL` and `CODE` to the appropriate values.

## Automatic

```shell
sh init.sh
```

## Manual checklist

* [ ] Log in with Apple ID
* [ ] Install Iterm2 profile using `iterm-nimo.json`

